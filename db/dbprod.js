// const mongoose = require("mongoose");
// require('dotenv').config()


// const connectProductDB = async () => {
//   try {
//     const connect = await mongoose.connect(process.env.MONGO_URI_PRODUCT);
//     console.log(`MongoDB connected on : ${connect.connection.host}`);
//   } catch (error) {
//     console.error("Product Database connection failed:", error.message); // Log the actual error message
//     throw new Error(`Database connection failed: ${error.message}`); // Provide more details in the thrown error


//   }
// };

// module.exports = connectProductDB;

